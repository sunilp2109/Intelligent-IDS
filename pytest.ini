[pytest]
pythonpath = . backend
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    unit: Isolated component tests
    integration: Cross-module pipeline tests
    api: HTTP API tests
    realtime: WebSocket tests
    performance: Local timing measurements
    fault: Graceful-failure tests
    security: Input validation and exposure tests
